<h1>todos</h1>
<div>

  <%= render 'form' %>

  <% @items.each do |item| %>
    <ul>
      <li><%= link_to item.title, item %> | <%= link_to 'edit', edit_item_path(item) %> | <%= link_to 'delete', item_path(item), method: :delete, data: {
        confirm: 'Are you sureeeeeee?'
      } %></li>
    </ul>
  <% end %>

</div>
